{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the PexliFormation main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "core:webview:allow-webview-window",
    "dialog:default",
    "opener:default",
    "shell:allow-open"
  ]
}
